-- =============================================================================
-- 20260507000010_bug_thread_id.sql
-- =============================================================================
-- Запоминаем топик (message_thread_id для forum-supergroup), в котором
-- пришёл баг — чтобы автообъявление о фиксе ушло в тот же топик, а не в
-- General. У нас один топик «Баги», но если когда-то будет «UI», «API» и
-- т.п. — каждое объявление улетит куда надо.
-- =============================================================================

alter table public.bug_reports
  add column if not exists telegram_thread_id bigint;
