-- =============================================================================
-- 20260514240000_profile_phone.sql
-- =============================================================================
-- profiles.phone — для отображения в /admin/users и в карточке пользователя.
-- Никакой валидации формата (хранится как ввёл юзер), formatPhone на клиенте.
-- =============================================================================

alter table public.profiles
  add column if not exists phone text;
