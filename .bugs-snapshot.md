# Open bugs: 10

## 301665aa [medium / visits] [+1 scr]

**Text:** карточка визитов вообще в окно браузера не вместилась
**AI summary:** Карточка визита не помещается в окно браузера — видимо, она слишком широкая или высокая. Пользователь не может полностью увидеть информацию о визите без прокрутки. Нужно сделать карточку адаптивнее или добавить скролл.

## 1b12beaf [high / payouts] [+1 scr]

**Text:** добавила один визит, везде выручку показывает, в ЗП нет
**AI summary:** Визит был добавлен и отображается в выручке, но не появился в расчётах зарплаты сотрудника. Похоже, данные визита не синхронизируются с модулем зарплаты. Нужно проверить, почему добавленный визит не учитывается при расчёте ЗП.

## a7267733 [- / -] [+1 scr]

## 32ac2c84 [medium / visits] [+1 scr]

**Text:** карточка визитов вообще в окно браузера не вместилась
**AI summary:** Карточка визитов не помещается в окно браузера, требуется прокрутка или переполнение контента.

## 5dce3ec8 [high / expenses] [+1 scr]

**Text:** добавила один визит, везде выручку показывает, в ЗП нет
**AI summary:** Добавленный визит отображается в выручке, но не учитывается в расчёте зарплаты сотрудника.

## 8603b637 [- / -] [+1 scr]

## 23e2d5ab [medium / visits] [+1 scr]

**Text:** карточка визитов вообще в окно браузера не вместилась
**AI summary:** Карточка визитов не помещается в окно браузера, требуется адаптация размера или скролл.

## c3e05b4a [high / visits] [+1 scr]

**Text:** карточка визитов вообще в окно браузера не вместилась
**AI summary:** Карточка визитов выходит за границы окна браузера, не помещается на экран.

## 0cc21375 [high / visits] [+1 scr]

**Text:** добавила один визит, везде выручку показывает, в ЗП нет
**AI summary:** После добавления визита выручка отображается везде, кроме расчёта заработной платы. Данные о визите не синхронизируются в модуль ЗП.

## 0ed685da [- / -] [+1 scr]
